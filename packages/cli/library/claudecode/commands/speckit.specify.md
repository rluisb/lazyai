---
description: Define what you want to build — requirements and user stories
argument-hint: "Build an application that helps users organize photos..."
allowed-tools: Read, Write, Bash, Grep, Glob
---

Load and follow the `speckit-specify` skill. The skill defines the full workflow.
