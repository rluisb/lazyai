# Spec 022 — Workflow Library Alignment with Speckit + Harness Engineering

**Status:** In Spec (updated after vault research) | **Date:** 2026-04-27

## Goal

Rebuild ai-setup's library (skills, agents, prompts, templates, fragments) to provide a comprehensive, tool-agnostic workflow engine that:

1. **Absorbs speckit methodology** as the core SDLC (Spec-Driven Development workflow)
2. **Adds complementary workflows** beyond spec-kit's scope (bugfix, spike, PoC, housekeeping, review, memory, self-improvement)
3. **Embeds Harness Engineering Protocol** concepts (Feed Forward, Dual-Agent Contract, Feedback & Sensors, Memory & State, Anti-Slope)
4. **Embeds prompt engineering techniques** (CoT, ToT, ReAct, Self-Consistency, Reflexion, Few-Shot, Prompt Chaining, LLM-as-Judge)
5. **Works across all supported tools** (Claude Code, OpenCode, Codex, Gemini, Copilot, Pi) — not just Claude Code
6. **Integrates with workspace engine** (multi-repo ledgers, standards propagation, planning repo awareness)
7. **Detects and respects existing spec-kit structures** — never overwrites, only adds what's missing
8. **Uses ai-setup's MCP ecosystem only** — no external paid services/CLIs

## Architecture Decision

**Option B: Adapt & Enhance** — speckit is the reference methodology, not a runtime dependency.

| Aspect | Decision |
|---|---|
| Speckit naming | Use `speckit-*` skill names for compatibility signaling |
| Speckit content | Absorb methodology, enhance with ai-setup concepts |
| Speckit CLI | NOT a dependency — ai-setup generates tool-native files via `compile` |
| Template format | Match speckit template structure, add ai-setup extensions |
| Prompt techniques | Embedded directly in skill instructions (NOT external plugins) |
| Harness Engineering | Woven into workflow phases as gates and checkpoints |
| Existing structures | Detect `.specify/` and `specs/###-slug/` — never overwrite, only add |

### Spec-kit structure awareness

When `ai-setup` detects an existing `.specify/` directory (spec-kit initialized), it:
- **Never overwrites** `.specify/memory/`, `.specify/templates/`, `.specify/scripts/`
- **Adds** workspace-specific files: `.specify/memory/repos/<name>/ledger.md` and `last-known-state.md`
- **Aligns** its library content to match the existing spec-kit template format for consistency

When `ai-setup` detects `specs/###-slug/` directories (bee-gone style), it:
- **Never restructures** existing spec directories
- **Adds** per-feature AGENTS.md if missing (pointing to the right spec templates)

### Output layers (generated by `ai-setup compile`)

| Tool | Output directory | File type | Generated from |
|---|---|---|---|
| Claude Code | `.claude/commands/` | `speckit.*.md` (slash commands) | `library/skills/speckit-*.md` |
| Copilot | `.github/agents/` + `.github/prompts/` | Agent + prompt files | `library/skills/` + `library/prompts/` |
| OpenCode | `.opencode/skills/` + `.opencode/commands/` | SKILL.md + command.md | `library/skills/` |
| Codex | `.codex/config.toml` + `.agents/skills/` | TOML + SKILL.md | `library/skills/` |
| Gemini | `.gemini/commands/` | TOML | `library/skills/` |
| Pi | `.pi/skills/` | SKILL.md | `library/skills/` |

### MCP tools (no external paid services)

All workflows use ai-setup's existing MCP ecosystem:

| MCP Tool | Used by | Purpose |
|---|---|---|
| **graphify** | Spike, Research, Self-Improvement | Query knowledge graph for related concepts, find connections |
| **codegraph** | Review, Bugfix, Housekeeping | Understand code dependencies, trace execution paths |
| **qmd** | All workflows | Query markdown documents for structured data extraction |
| **obsidian** | Memory Update, Self-Improvement | Read/write to vault for persistent memory, standards |
| **filesystem** | All workflows | Direct file read/write for spec/plan/task documents |
| **ripgrep/grep** | Review, Bugfix, Spike | Search codebase for patterns |
| **memory** (ai-setup) | Memory Update | Durable state snapshots, workspace ledgers |

---

## Prompt Architecture Standard

### Every skill follows the 9-section blueprint (from vault)

All skill files in `library/skills/` follow this structure:

```markdown
---
name: speckit-specify
description: Create a feature specification from a user description
argument-hint: "Build a photo album organizer that..."
techniques: [chain-of-thought, few-shot, feed-forward]
output: spec.md
output_schema:
  sections: [User Scenarios, Requirements, Key Entities, Success Criteria, Edge Cases, Assumptions]
consumes: [constitution.md]
produces_for: [speckit-clarify, speckit-plan]
mcp_tools: [filesystem, qmd, ripgrep]
---

# 1. IDENTITY AND ROLE
You are a specification author...

# 2. PERSONALITY AND TONE
Methodical, precise, user-focused...

# 3. KNOWLEDGE AND SPECIALTIES
- User story decomposition
- Acceptance criteria definition
- Boundary analysis

# 4. RESPONSE STYLE
Write to `specs/{feature-number}-{slug}/spec.md`...

# 5. SPECIFIC GUIDELINES
- Always read constitution.md first
- Every user story MUST be independently testable
- Use `<cot>` tags for internal reasoning

# 6. LIMITATIONS
- Do NOT define tech stack (that's the plan phase)
- Do NOT create tasks (that's the tasks phase)
- Escalate to human if scope is unclear

# 7. DATA
<data>
## Spec Template
... template content ...
</data>

# 8. FEW-SHOT EXAMPLES
<example>
User: Build a task manager
Assistant: [produces spec.md with P1→P3 user stories]
</example>

# 9. CHAIN OF THOUGHTS
<cot>
Use Chain-of-Thought when decomposing user stories:
1. Identify primary user and goal
2. Enumerate user steps
3. Define acceptance criteria
</cot>
```

### Reasoning-model variant

Skills also include a **GPT-5 / Claude Opus variant** using the concise format:

```
Role → Task → Context → Verification Steps → Rules → Expected Output
```

---

## Output Contract System

Each workflow has explicit input/output contracts that define how workflows chain together:

| Workflow | Output file | Output schema (sections) | Consumes | Produces for |
|---|---|---|---|---|
| speckit-constitution | `constitution.md` | Principles, Constraints, Governance | None | All workflows |
| speckit-specify | `specs/###-slug/spec.md` | User Scenarios, FR-*, Entities, Success Criteria, Edge Cases, Assumptions | constitution.md | clarify, plan |
| speckit-clarify | Updates spec.md | Clarifications section | spec.md | plan |
| speckit-plan | `specs/###-slug/plan.md` + `research.md` + `data-model.md` + `quickstart.md` | Summary, Tech Context, Constitution Check, Structure, Complexity | spec.md, constitution.md | tasks, analyze |
| speckit-tasks | `specs/###-slug/tasks.md` | Phases, [P] markers, [US*] labels, Dependencies | plan.md, spec.md | analyze, implement |
| speckit-analyze | Analysis report (inline or file) | Consistency gaps, Coverage gaps | spec.md, plan.md, tasks.md | implement |
| speckit-checklist | `specs/###-slug/checklists/` | Per-requirement validation items | spec.md, plan.md | implement |
| speckit-implement | Code changes + commits | Per-task implementation | tasks.md, plan.md | review, memory-update |
| rpi | `plan.md` + code changes | Lighter than full SDD | constitution.md | review, memory-update |
| bugfix | RCA + fix | Root cause, Hypothesis, Fix, Verification | codegraph | review, memory-update |
| spike | `research/` output | Multi-path analysis, Recommendation | graphify, qmd | plan, speckit-specify |
| proof-of-concept | PoC code + validation | Feasibility report, Throwaway code | graphify | speckit-specify |
| housekeeping | Updated code + changelog | Dependency versions, Tech debt resolved | codegraph | memory-update |
| review | Review report | Findings by severity, Per-lens analysis | codegraph, qmd | memory-update |
| extract-standards | Updated `standards/` files | Pattern → Standard mapping | codegraph, qmd | constitution |
| update-memory | `ledger.md` + `last-known-state.md` | Date, Who, What, Plan ref, Verified | obsidian, memory | None |
| self-improve | Memory updates | Improvement patterns identified | graphify, obsidian | update-memory |
| process-audit | Audit report | Process adherence metrics, Suggestions | qmd, memory | update-memory |

---

## Workflow Catalog

### Tier 1 — Core SDLC

#### WF-01: Spec-Driven Development (SDD)
- **Purpose:** Full feature development from spec to implementation
- **Speckit alignment:** `/speckit.specify` → `/speckit.clarify` → `/speckit.plan` → `/speckit.tasks` → `/speckit.checklist` → `/speckit.analyze` → `/speckit.implement`
- **When to use:** New feature, greenfield, brownfield with significant scope
- **Prompt techniques:** Chain-of-Thought (specify phase), ReAct (plan + implement), Prompt Chaining (pipeline), Self-Consistency (analyze)
- **Harness Engineering:** Feed Forward (spec blueprint), Contract (analyze verification), Feedback (checklist validation), Memory (ledger update on completion)
- **Skills produced:** `speckit-specify`, `speckit-clarify`, `speckit-plan`, `speckit-tasks`, `speckit-analyze`, `speckit-checklist`, `speckit-implement`
- **Templates produced:** `spec-template.md`, `plan-template.md`, `tasks-template.md`, `checklist-template.md`

#### WF-02: RPI (Research → Plan → Implement)
- **Purpose:** Lighter-weight version of SDD for non-trivial but bounded tasks
- **Speckit alignment:** Subset of SDD — skip full spec.md, keep plan.md + tasks + implement
- **When to use:** Task that requires research and planning but not a full feature spec (e.g., "refactor auth middleware", "add rate limiting")
- **Prompt techniques:** ReAct (research + plan interleaved), Few-Shot (task examples), Decision Protocol (multiple approaches)
- **Harness Engineering:** Feed Forward (constrained scope), Contract (acceptance criteria), Feedback (quality gates per task)
- **Skills produced:** `rpi` (updated — orchestration skill that chains research + plan + implement)
- **Templates used:** Reuses plan-template.md, task.md

#### WF-03: Bugfix
- **Purpose:** Root cause analysis + minimal surgical fix
- **Speckit alignment:** None — this is a separate, lighter workflow
- **When to use:** Bug reports, issues that don't require re-specification
- **Prompt techniques:** Trace execution (follow the bug through the code), Hypothesis testing (propose → verify → fix), Few-Shot (similar bug patterns)
- **Harness Engineering:** Contract (expected vs actual behavior), Feedback (test the fix, verify no regression), Memory (update bugfix ledger)
- **Skills produced:** `bugfix`
- **Templates produced:** `bugfix-rca-template.md` (update existing)

#### WF-04: Spike / Deep Research
- **Purpose:** Multi-round research with different perspectives to reduce uncertainty before committing to a plan
- **Speckit alignment:** Enhanced version of `/speckit.clarify` + `/speckit.plan` research phase
- **When to use:** High uncertainty, new technology, architectural decisions
- **Prompt techniques:** Tree-of-Thoughts (explore multiple paths, evaluate, backtrack), Self-Consistency (multiple perspectives, consensus), Generated Knowledge (pre-generate context), Reflexion (self-evaluate findings)
- **Harness Engineering:** Feed Forward (research scope and questions upfront), Dual-Agent (two perspectives on same problem), Memory (capture findings)
- **Skills produced:** `spike`
- **Templates produced:** `spike-template.md`

#### WF-05: Proof-of-Concept (PoC)
- **Purpose:** Validate feasibility with minimal implementation — shallower than full RPI
- **Speckit alignment:** Mini-RPI — research → lightweight plan → spike implementation
- **When to use:** "Can we do X with Y?", technology evaluation, prototype
- **Prompt techniques:** Few-Shot (similar PoC patterns), Chain-of-Thought (feasibility reasoning), Generated Knowledge (pre-research)
- **Harness Engineering:** Feed Forward (success criteria for PoC), Contract (what "validated" means), Anti-Slope (throw away the PoC — don't ship it)
- **Skills produced:** `proof-of-concept`
- **Templates produced:** `poc-template.md`

#### WF-06: Housekeeping
- **Purpose:** Dependency updates, tech debt resolution, cleanup — structured maintenance
- **Speckit alignment:** None — maintenance workflow
- **When to use:** Scheduled maintenance, version bumps, cleanup runs
- **Prompt techniques:** Structured Output (checklist format), Few-Shot (common maintenance patterns)
- **Harness Engineering:** Feed Forward (maintenance scope), Feedback (verify after each change), Memory (update tech-debt ledger)
- **Skills produced:** `housekeeping`
- **Templates produced:** `housekeeping-template.md`

### Tier 2 — Quality & Governance

#### WF-07: Review (Parallel + Judge)

Uses **LLM-as-Judge pattern** from vault (`concept-LLM-as-Judge.md`): multiple review lenses run in parallel, a judge synthesizes findings.

```
                    ┌─ Lens: Security ──────┐
START → Decompose ─┼─ Lens: Performance ───┼─ JUDGE (synthesizer) → Report → END
                    └─ Lens: Correctness ───┘
```

- **Judge prompt:** "Evaluate responses from three review lenses. Identify conflicts, prioritize by severity, produce a single structured review report. Be objective. Cite source files."
- **MCP tools:** `codegraph` to verify dependency claims, `qmd` to cross-reference with specs
- **Prompt techniques:** Dual-Agent Simulation, Self-Consistency, LLM-as-Judge
- **Harness Engineering:** Contract (check against spec/plan), Feedback (actionable findings with severity)
- **Skills produced:** `review` (rebuild existing)
- **Templates produced:** `code-review-template.md` (update existing)

#### WF-08: Code Standards Update
- **Purpose:** Extract patterns from codebase → update project/workspace/global standards
- **Speckit alignment:** None — meta-governance workflow
- **MCP tools:** `codegraph` for dependency analysis, `qmd` for existing standards query, `obsidian` for vault persistence
- **Prompt techniques:** Few-Shot (pattern recognition), Reflexion (compare current vs desired), Self-Consistency (check across repos)
- **Harness Engineering:** Feedback (patterns observed in reality), Memory (standards as durable memory), Contract (standards as constraints)
- **Skills produced:** `extract-standards` (updated — workspace-aware)
- **Templates produced:** `standard.md` (update existing)

#### WF-09: Memory Update
- **Purpose:** Update project/workspace memory from completed work — feeds `.specify/memory/` and `specs/memory/repos/`
- **Speckit alignment:** None — post-workflow housekeeping
- **MCP tools:** `obsidian` to write to vault, `memory` (ai-setup) for durable state
- **Harness Engineering:** Memory & State (beating amnesia — durable record), Feedback (lessons learned)
- **Skills produced:** `update-memory` (new, replaces/extends `memory-write`)
- **Templates produced:** `ledger-template.md`, `last-known-state-template.md`

#### WF-10: Constitution Check
- **Purpose:** Verify against project constitution — embedded in `speckit-plan` and `speckit-implement`
- **Speckit alignment:** Enhanced version of speckit's constitution check phase
- **Prompt techniques:** Dual-Agent (plan vs constitution), Structured Output (pass/fail per principle)
- **Harness Engineering:** Contract (constitution as governing contract), Feedback (violations detected)

### Tier 3 — Meta

#### WF-11: Self-Improvement
- **Purpose:** AI reviews its own output across sessions, identifies improvement patterns
- **MCP tools:** `graphify` for concept connections, `obsidian` for vault persistence
- **Prompt techniques:** Reflexion (self-evaluate), ART (analyze own traces)
- **Harness Engineering:** Feedback (objective self-assessment), Memory (persist learnings), Anti-Slope (prevent regression)
- **Skills produced:** `self-improve`

#### WF-12: Process Audit
- **Purpose:** Reviews workflow adherence, suggests process improvements
- **Prompt techniques:** Self-Consistency (check across sessions), Structured Output (audit report)
- **Harness Engineering:** Feedback & Sensors (measure process effectiveness), Memory (process metrics)
- **Skills produced:** `process-audit`
- **Templates produced:** `audit-template.md`

#### WF-13: Human-in-the-Loop (Optimizer Pattern)

Formalized from vault's LangGraph Optimizer pattern (uses `interrupt()` to pause for human review):

```
START → generate → evaluate (INTERRUPT for human)
  → [rejected with feedback: loop back to generate]
  → [approved: proceed to next phase]
→ END
```

Every `⛔ HUMAN GATE` in any workflow follows this pattern with explicit:
- What the human evaluates (spec, plan, review findings)
- What happens on rejection (loop back with feedback)
- What happens on approval (proceed)

---

## Template Alignment

| Current ai-setup template | → | New template | Changes |
|---|---|---|---|
| `templates/spec-template.md` | → | `templates/spec-template.md` | Full rewrite to speckit format (User Scenarios, FR-*, Success Criteria, Edge Cases, Assumptions) |
| `templates/plan-template.md` | → | `templates/plan-template.md` | Full rewrite to speckit format (Technical Context, Constitution Check, Project Structure, Complexity Tracking) |
| `templates/task.md` | → | `templates/tasks-template.md` | Full rewrite to speckit format (Phases, User Story grouping, [P] parallel markers, Dependencies) |
| `templates/checklist-template.md` | → | `templates/checklist-template.md` | Align with speckit quality gate format |
| `templates/adr.md` | → | Keep, minor update | Add speckit constitution reference |
| `templates/bugfix-rca-template.md` | → | Update | Add Harness Engineering Contract section |
| `templates/standard.md` | → | Update | Workspace-aware standards format |
| None | → | `templates/spike-template.md` | New |
| None | → | `templates/poc-template.md` | New |
| None | → | `templates/housekeeping-template.md` | New |
| None | → | `templates/ledger-template.md` | New (currently generated inline) |
| None | → | `templates/audit-template.md` | New |

---

## Agent Updates

| Agent | Changes |
|---|---|
| `planner` | Add speckit awareness — reads constitution, spec, generates speckit-compatible plans |
| `builder` | Add workspace awareness — respects per-repo permissions, updates ledgers |
| `reviewer` | Add LLM-as-Judge synthesizer mode + multi-lens (security, perf, correctness) |
| `scout` | Add speckit-aware research — searches existing specs for related work |
| `red-team` | Add Dual-Agent Contract mode — adversarial spec/code verification |
| `orchestrator` | Add Multi-Agent Orchestrator topology: decomposes → routes to workers → synthesizes |

### Orchestrator topology (from vault `concept-Multi-Agent-Orchestration`)

```
Orchestrator (decomposes task into JSON, routes to workers)
  ├── Worker 1: Analysis & Research   (scout agent)
  ├── Worker 2: Development & Build   (builder agent)
  └── Worker 3: Validation & Review   (reviewer agent)
→ Synthesizer (merges, deduplicates, produces final output)
```

---

## Fragment Updates

| Current fragment | → | New fragment | Changes |
|---|---|---|---|
| `rpi-workflow.md` | → | `rpi-workflow.md` | Rebuild with Feed Forward → Research → Plan → Implement → Feedback phases |
| `reasoning-protocol.md` | → | Update | Add CoT, ReAct, ToT, LLM-as-Judge technique references |
| `decision-protocol.md` | → | Keep | Already good |
| `context-discipline.md` | → | Keep | Already good |
| `quality-gates.xml` | → | Update | Add speckit constitution check |
| None | → | `harness-protocol.md` | New — Feed Forward, Contract, Feedback, Memory, Anti-Slope with concrete case studies |
| None | → | `workspace-protocol.md` | New — multi-repo awareness, ledger updates, standards propagation |

### Harness Engineering References (from vault `28-Harness-Engineering-Implementation-Examples`)

| Harness Rule | Best Implementation Reference |
|---|---|
| Feed Forward | Spec Kit chain (constitution → specify → plan → tasks) |
| The Contract | Dual-agent evaluator (generator + verifier, LLM-as-Judge) |
| Feedback & Sensors | OpenTelemetry + canary analysis pattern |
| Memory & State | LangGraph durable checkpoints + ledger pattern |
| Anti-Slope | GitHub branch protections + CI gate ladder |

---

## Library Structure (target)

```
library/
├── skills/
│   ├── speckit-constitution.md      # NEW
│   ├── speckit-specify.md           # NEW
│   ├── speckit-clarify.md           # NEW
│   ├── speckit-plan.md              # REBUILT from plan.md
│   ├── speckit-tasks.md             # NEW
│   ├── speckit-analyze.md           # NEW
│   ├── speckit-checklist.md         # NEW
│   ├── speckit-implement.md         # REBUILT from implement.md
│   ├── rpi.md                       # UPDATED — orchestration chain
│   ├── bugfix.md                    # NEW
│   ├── spike.md                     # NEW
│   ├── proof-of-concept.md          # NEW
│   ├── housekeeping.md              # NEW
│   ├── review.md                    # REBUILT — LLM-as-Judge + multi-lens
│   ├── extract-standards.md         # UPDATED — workspace-aware
│   ├── update-memory.md             # NEW — replaces memory-write
│   ├── self-improve.md              # NEW
│   ├── process-audit.md             # NEW
│   ├── tdd-loop.md                  # KEEP
│   ├── parallel-execution.md        # KEEP
│   └── anti-speculation.md          # KEEP
├── agents/
│   ├── builder.md                   # UPDATED — workspace + speckit aware
│   ├── documenter.md                # KEEP
│   ├── orchestrator.md              # UPDATED — Orchestrator→Workers→Synthesizer
│   ├── planner.md                   # UPDATED — speckit-aware
│   ├── red-team.md                  # UPDATED — Dual-Agent Contract mode
│   ├── reviewer.md                  # UPDATED — LLM-as-Judge + multi-lens
│   └── scout.md                     # UPDATED — speckit-aware research
├── prompts/
│   ├── speckit-specify.prompt.md    # NEW
│   ├── speckit-plan.prompt.md       # NEW
│   ├── speckit-tasks.prompt.md      # NEW
│   ├── speckit-implement.prompt.md  # NEW
│   ├── spike.prompt.md              # NEW
│   ├── review.prompt.md             # NEW
│   ├── compact.md                   # KEEP
│   └── plan.md                      # KEEP (legacy)
├── templates/
│   ├── spec-template.md             # REPLACED — speckit format
│   ├── plan-template.md             # REPLACED — speckit format
│   ├── tasks-template.md            # REPLACED — speckit format
│   ├── checklist-template.md        # UPDATED
│   ├── spike-template.md            # NEW
│   ├── poc-template.md              # NEW
│   ├── housekeeping-template.md     # NEW
│   ├── ledger-template.md           # NEW
│   ├── audit-template.md            # NEW
│   ├── adr.md                       # KEEP — minor update
│   ├── bugfix-rca-template.md       # UPDATED
│   ├── standard.md                  # UPDATED
│   └── code-review-template.md      # UPDATED
├── fragments/
│   ├── rpi-workflow.md              # REBUILT — Harness phases
│   ├── harness-protocol.md          # NEW — with concrete case studies
│   ├── workspace-protocol.md         # NEW
│   ├── reasoning-protocol.md        # UPDATED — CoT, ReAct, ToT, LLM-as-Judge
│   ├── decision-protocol.md         # KEEP
│   ├── context-discipline.md        # KEEP
│   ├── quality-gates.xml            # UPDATED — speckit constitution check
│   ├── agent-harness.md             # KEEP
│   ├── bug-resolution.xml           # KEEP
│   ├── git-conventions.xml          # KEEP
│   ├── git-safety.md                # KEEP
│   └── system-context.xml           # KEEP
└── constitution/
    ├── constitution.template.md     # REPLACED — single file, speckit-aligned
    ├── constraints.template.md      # MERGE into constitution
    ├── quality-gates.template.md    # MERGE into constitution
    └── uncertainty.template.md      # MERGE into constitution
```

---

## Acceptance Criteria

1. **All 13 workflows have corresponding skills** in `library/skills/`
2. **All speckit-aligned templates** exist in `library/templates/` with matching format
3. **Each skill** declares techniques, output schema, consumed files, and MCP tools in frontmatter
4. **All skills follow the 9-section blueprint** (Identity → CoT) with reasoning-model variant
5. **Each workflow** embeds Harness Engineering concepts at the appropriate phase
6. **Output contracts** enable Prompt Chaining — each skill's output is the next skill's input
7. **All agents** are updated for speckit + workspace awareness + LLM-as-Judge
8. **Orchestrator agent** implements the Orchestrator→Workers→Synthesizer topology
9. **RPI fragment** is rebuilt with Feed Forward → Research → Plan → Implement → Feedback
10. **Human-in-the-Loop** is formalized as a first-class pattern in all human-gated workflows
11. **Workspace integration** — skills reference planning repo, per-repo ledgers, standards propagation
12. **Existing spec-kit structures detected and respected** — `.specify/` and `specs/###-slug/` never overwritten
13. **Tool-native output** — `ai-setup compile` generates correct files for all 6+ tools
14. **MCP tools only** — no external paid services/CLIs referenced in any skill
15. **Existing tests continue to pass** — no regression in scaffold, compile, or adapter logic
16. **New tests** cover: skill frontmatter validation, template format validation, output contract validation, workspace-aware skill behavior
