# Checklist: Phase 1 Adapter Decouple and Test Rewrite

**Phase:** Phase 1  
**Exit:** Adapter behavior is neutral, tested, and no longer depends on Fortnite assumptions.

## Preconditions

- [x] Phase 0 approval evidence is tracked.
- [x] `tasks.md`, `analysis.md`, and all phase checklists are reviewed.
- [x] Registered adapter scope is unchanged: OpenCode, Claude Code, Copilot only.
- [x] Baseline theme fixture is repaired or the baseline failure is explicitly isolated from Phase 1 evidence.

## RED Tests

- [ ] OpenCode contract test fails on current `FortniteMode` / `loop-driver` / `STARTUP.md` behavior.
- [ ] Claude Code contract test fails on any orchestrator/Fortnite reference.
- [ ] Copilot contract test fails on any orchestrator/Fortnite reference.
- [ ] Session/default tests fail while `loop-driver` or `orchestrator` defaults remain.

## Implementation Checks

- [x] OpenCode default agent is `primary-agent`.
- [x] Claude Code default agent is `primary-agent`.
- [x] Copilot default agent is `primary-agent`.
- [x] `FortniteMode` is removed from adapter context construction and install behavior.
- [x] `STARTUP.md` is not generated by the neutral adapter path.
- [x] Runtime/session defaults use `primary-agent`.
- [x] Config defaults use `primary-agent`.
- [x] No hidden fallback preserves `loop-driver` or `orchestrator`.

## Verification

- [x] `go test ./packages/cli/internal/adapter -run 'OpenCode|Claude|Copilot|Adapter' -count=1` passes.
- [x] `go test ./packages/cli/internal/runtime/session -count=1` passes.
- [x] `go test ./packages/cli/cmd -run 'Session|Config|Helpers|Init' -count=1` passes.
- [x] Search confirms no Phase 1-owned default references remain: `FortniteMode`, `loop-driver`, `default_agent: "orchestrator"`.

## Rollback Record

- [x] Equivalent tracked rollback point exists: commit `98e83d24cf5aefb64a51b3a6e3370bfac5369d4d` is the last tracked pre-implementation point.
- [x] Rollback command from `rollback.md` is valid with the recorded coarse rollback point.
- [x] Phase 1 verification output is recorded.

## Gate

- [x] Human approves adapter contract evidence before Phase 2 excision begins.
