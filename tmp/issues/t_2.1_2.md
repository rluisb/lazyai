Parent epic: Cycle 2.1.

Decide whether the two new templates belong in curation/provenance, then apply per repo convention.

### Scope
Inspect `packages/cli/library/manifests/curation.yaml` and `provenance.yaml` conventions first. If conventions require it, register:
- `packages/cli/library/templates/skill-quality.md`
- `packages/cli/library/templates/agent-contract.md`

### Acceptance
- Manifest validation still passes.
- Templates discoverable by library tooling.
- If convention does NOT require registration, document that decision in the PR.

### Boundary
Backward-compatible manifest edits only.
