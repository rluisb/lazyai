Parent epic: Cycle 2.1.

Produce a warning inventory by running semantic validation against shipped library assets (use existing safe path only).

### Scope
Capture per finding: rule id, asset path, count, likely true/false positive, recommended future cleanup. Store at `docs/reports/semantic-validation-warning-inventory.md` (follow existing report conventions).

### Acceptance
- No mass asset rewrite.
- Inventory gives a future cleanup path.
- False positives explicitly noted.

### Boundary
Inventory only — no bulk rewrites this cycle.
