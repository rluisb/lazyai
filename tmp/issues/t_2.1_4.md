Parent epic: Cycle 2.1.

Refine semantic-validation message wording only where clarity demands it.

### Scope
`packages/cli/internal/validate/validate.go` (+ `validate_test.go`). Adjust human-facing wording without changing rule semantics.

### Acceptance
- Rule IDs stay stable.
- `go test ./packages/cli/internal/validate/...` passes.

### Boundary
No new rules, no behavior change beyond wording.
