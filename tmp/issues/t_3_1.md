Parent epic: Cycle 3.

Author a neutral hook lifecycle catalog that does NOT imply LazyAI executes hooks.

### Scope
Add/update `packages/cli/library/hooks/catalog.md` and `docs/concepts/hook-lifecycle.md`. Lifecycle: before_agent, before_model, before_tool, after_tool, after_model, after_agent, on_error, on_compaction, on_handoff, on_human_gate.

### Acceptance
- Lifecycle documented.
- Host-tool execution boundary explicit (LazyAI compiles assets; host tools run them).

### Boundary
Docs/catalog only. No scheduler.
