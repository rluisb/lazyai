Parent epic: Cycle 3.

Map hook support honestly per adapter.

### Scope
Capability values: supported, partial, instruction_only, unsupported, not_applicable — for opencode, claude, copilot, pi, omp, antigravity, kiro. Cross-check against `packages/cli/internal/adapter/capabilities.go` + `output_mapping.go`.

### Acceptance
- Weak surfaces use instruction-only fallbacks, not fake hooks.
- Matrix matches adapter code.

### Boundary
Mapping/docs. No new runtime.
