Parent epic: Cycle 3.

Add validation + tests for hook assets (or docs-only fallback).

### Scope
Note: `packages/cli/internal/hooks/` does not exist yet. If code support is appropriate, add `lifecycle.go`/`capabilities.go` + checks for: unknown lifecycle, unsupported adapter claim, missing purpose, missing safety behavior for blocking hooks. If validation is out of scope, ship docs + capability-mapping tests only.

### Acceptance
- Tests pass.
- No runtime hook scheduler added.

### Boundary
Validation only — never execution.
