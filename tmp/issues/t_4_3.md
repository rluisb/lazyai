Parent epic: Cycle 4.

Add first-class rubric assets validated against the existing schema.

### Scope
Create `packages/cli/library/rubrics/` (net-new): reviewer, evidence-verifier, planner, implementer, skill-quality, hook-quality rubrics, validated against `packages/cli/internal/schema/eval-rubric.schema.json`. Cover correctness, evidence, verification, security, minimality, convention fit, human gate, handoff quality, uncertainty.

### Acceptance
- Rubrics discoverable; existing eval/rubric validation passes.
- No LLM-as-judge runtime.
