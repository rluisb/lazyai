Parent epic: Cycle 6.

Clarify init headless populate behavior.

### Scope
`packages/cli/cmd/init.go` (+ `README.md`, `docs/commands/init.md` if created). Explain why AGENTS.md placeholder fill may be skipped, what the host tool should do next, how to complete setup manually, how to validate after populate.

### Acceptance
- No hidden runtime behavior added.
- CLI explains what happened + next steps.
