Parent epic: Cycle 6.

Clarify server L1/L3 capability levels honestly.

### Scope
`packages/cli/cmd/server.go` (+ README, `docs/commands/server.md` if created, doctor/status messaging). Distinguish L1 config check vs L3 handshake (not implemented in Go), Node.js MCP SDK dependency if still true, current limitation vs future work.

### Acceptance
- Users can distinguish L1 config check vs L3 handshake.
- No fake support claimed.
